< 설명 >
n이 양의 정수 x의 제곱이라면 x+1의 제곱을 리턴하고, 
n이 양의 정수 x의 제곱이 아니라면 -1을 리턴하는 함수를 완성하세요.

< 예시 >
121	144
3	-1

function solution(n) {
    return Number.isInteger(Math.sqrt(n)) > 0? (Math.sqrt(n)+1)*(Math.sqrt(n)+1) : -1
}
