< 설명 >
자연수 n을 뒤집어 각 자리 숫자를 원소로 가지는 배열 형태로 리턴해주세요.

< 예시 >
12345	[5,4,3,2,1]

function solution(n) {
    return n.toString().split("").reverse().map(val=>parseInt(val))
}

-> reverse(): 배열을 반대로 --> sort(reverse = true) X
-> parseInt(): String을 Number로
